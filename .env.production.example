# eBay Price Reducer - Production Environment Variables
# Copy this file to .env.production and fill in your actual values

# =============================================================================
# FRONTEND ENVIRONMENT VARIABLES (VITE_ prefix for client-side access)
# =============================================================================

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Application Configuration
VITE_APP_NAME=eBay Price Reducer
VITE_APP_VERSION=1.0.0
VITE_API_BASE_URL=https://your-app-name.netlify.app/.netlify/functions

# =============================================================================
# BACKEND ENVIRONMENT VARIABLES (Netlify Functions)
# =============================================================================

# Node Environment
NODE_ENV=production

# Database Configuration
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# Authentication
JWT_SECRET=your_cryptographically_secure_32_character_secret_here

# eBay API Configuration (Production)
EBAY_APP_ID=your_production_ebay_app_id
EBAY_DEV_ID=your_production_ebay_dev_id
EBAY_CERT_ID=your_production_ebay_cert_id
EBAY_USER_TOKEN=your_production_ebay_user_token
EBAY_ENVIRONMENT=production
EBAY_SITE_ID=0
EBAY_API_VERSION=967

# Security Configuration
CORS_ORIGIN=https://your-custom-domain.com
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Application Configuration
APP_NAME=eBay Price Reducer
APP_VERSION=1.0.0

# Logging Configuration
LOG_LEVEL=info

# Optional: Email Notifications
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_app_password

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================

# Custom Domain (if using)
CUSTOM_DOMAIN=your-custom-domain.com

# Monitoring
HEALTH_CHECK_URL=https://your-app-name.netlify.app/api/health

# =============================================================================
# SECURITY NOTES
# =============================================================================

# 1. NEVER commit this file with real values to version control
# 2. Use strong, unique passwords and secrets
# 3. Rotate credentials regularly
# 4. Use environment-specific credentials
# 5. Monitor for credential leaks

# =============================================================================
# VALIDATION CHECKLIST
# =============================================================================

# Before deploying to production, ensure:
# [ ] All placeholder values are replaced with real credentials
# [ ] JWT_SECRET is at least 32 characters and cryptographically secure
# [ ] eBay credentials are for production environment (not sandbox)
# [ ] Supabase URLs and keys are for production project
# [ ] CORS_ORIGIN matches your actual domain
# [ ] Email credentials are valid (if using email features)
# [ ] All secrets are stored securely in Netlify environment variables

# =============================================================================
# GETTING CREDENTIALS
# =============================================================================

# Supabase:
# 1. Go to https://supabase.com/dashboard
# 2. Select your production project
# 3. Go to Settings > API
# 4. Copy Project URL and API Keys

# eBay:
# 1. Go to https://developer.ebay.com/
# 2. Create a production application
# 3. Get your production keys from the developer console
# 4. Generate a user token for production use

# JWT Secret:
# Generate a secure random string (32+ characters):
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"